server:
  port: 18081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.27.178.1:1081/zjfx?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: eic
    password: ZOge!jOE6uqV
    hikari:
      pool-name: DateHikariCP
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.Mapper.xml
  type-aliases-package: com.zzwei.server.pojo
  configuration:
    #自动驼峰命名
    map-underscore-to-camel-case: false

logging:
  level:
    com.zzwei.server.mapper: debug

jwt:
  tokenHeader: Authorization
  secret: yeb-secret
  expiration: 604800
  tokenHead: Bearer